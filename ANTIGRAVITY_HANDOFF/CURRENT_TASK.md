# Current Task — Full Local Engineering One-Shot

The owner selected `LARGE_GOAL_PROMPT.md` for one dependency-gated local/prepared
implementation run across MAP-016 through MAP-025.

Execute every phase in that script sequentially. Production/provider/owner gates
remain blocked and must not be bypassed, but they do not stop independent local
preparation in later phases. Preserve all Codex fail-closed corrections and the
dirty worktree. Deliver actual code, executed tests, checkpoints 01–09, and one
complete evidence report for later Codex review.

`MASTER_ACTION_PLAN.md` remains the only backlog. No production mutation, push,
merge, deployment, owner-policy invention, MAP deletion, or launch claim is
authorized.
