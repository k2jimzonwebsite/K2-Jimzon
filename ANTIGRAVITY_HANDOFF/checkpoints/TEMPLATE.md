# Phase checkpoint: [phase and MAP item]

## Result

Use exactly one: `Ready for independent verification`, `Partially implemented`,
or `Blocked — evidence required`.

## Scope and dependency gate

- MAP requirements addressed:
- Earlier dependency evidence relied upon:
- Owner decisions required:
- Work deliberately excluded:

## Current-state due diligence

- Code/schema/provider state inspected:
- Dirty-worktree preservation:
- Problem reconfirmed from:

## Changes and files

List every file changed in this phase and its precise purpose. Separate prior
user changes from phase changes.

## Verification

| Exact command or provider check | Exit/result | Behavior proven | Evidence level |
| --- | --- | --- | --- |

Evidence level must be one of: `Not checked`, `Prepared locally`,
`Validated in rollback-only production transaction`, `Applied permanently`,
`Configured in provider`, `Deployed`, or `Verified on real production host`.

## Denial, failure, and recovery evidence

- Permission/ownership/IDOR denial:
- Invalid/unknown/oversized input:
- Duplicate/concurrent/replay behavior:
- Timeout/retry/recovery:
- Transaction/data rollback:
- Safe errors/log redaction:

## UI and accessibility evidence

Use `Not changed` when applicable. Otherwise record phone, tablet, desktop,
keyboard/focus, text zoom, theme, reduced motion, loading, empty, partial,
permission, conflict, error, retry, and recovery results.

## Provider and production truth

State separately what is local, rollback-validated, applied, configured,
deployed, real-host verified, manual, disabled, unavailable, or not checked.

## Rollback

- Code/config rollback:
- Migration/data rollback:
- What was actually rollback-tested:

## Remaining blockers and next safe phase

- Failed or skipped checks:
- Exact unblock condition:
- Next phase safe to begin:

## Truth statement

`No claim above exceeds its evidence.`
