# Admin phone layout source checkpoint

These `.before.txt` copies preserve the exact pre-edit `Admin.jsx`, `AdminWorkspaceUi.jsx` and `index.css` from the shared feature branch on 24 September 2026. They include earlier work in the dirty workspace; they are not the production baseline.

For scoped recovery, compare the current files to these copies and restore only the phone navigation, toolbar, shared text-fit and Admin button changes from IDEA-20260924-03. Keep unrelated payment and SEO edits. No provider or database rollback belongs to this UI slice.
