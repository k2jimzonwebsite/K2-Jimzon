# Storefront audit repair  -  24 September 2026

**Owner request and authority:** After the full website audit, fix the identified customer journey gaps. This is IDEA-20260924-04 in the idea register and S-01-S-08 in MAP-028, with activation and acceptance owned by MAP-019/020/023/024/025. The two attached QR screenshots are receiving details, not instructions to report a completed transfer.

**Prepared source:** The receipt now shows only its recorded GCash or MariBank method. Home FAQ and public payment/privacy policies describe the current manual transfer sequence and optional account data. Product metadata honors the prelaunch noindex gate; the allergen notice has one `Contains` prefix. The optional account flow includes first-use passwordless sign-in, editable name/address/in-app preference, empty-field checkout prefill, unread count and read action. Storefront BFF routes and a private SQL migration supply signed, user-scoped settings and reference-only notifications from canonical staff reply, order, verified-payment and Pasabuy changes. The new migration is prepared only; do not apply it before the MAP-019/020 guest/account prerequisites and coordinated cutover.

**Changed systems and files:** Storefront account/context/UI/service, receipt, checkout, metadata, product and policy files under `src/`; `prepared-api/storefront/account/settings.js` and `notifications.js`; `server/storefront-bff/router.js`; `supabase/migrations/20260924_customer_account_settings_notifications.sql`; isolated SQL fixtures/rehearsal; focused source and browser tests. Architecture and current-state records are in `docs/ARCHITECTURE.md`, `docs/PROJECT_MAP.md`, the K2 operations rulebook, System Brain and MAP-028.

**Local evidence:** `npx playwright test --config=playwright.api.config.js tests/customer-account-settings-contract.spec.js tests/guest-commerce-bff-contract.spec.js tests/storefront-auth-boundary.spec.js tests/storefront-copy-contract.spec.js tests/prelaunch-indexing-contract.spec.js` passed 37/37. `npm run rehearse:map019-account-claim` passed isolated PostgreSQL rollback, migration apply, behavior and replay, including settings/notification ownership checks. `npm run test:customer-account-ui` passed 4/4 (verified new account, phone sign-in, linked history/reply and wholesale boundary). `npm run build:storefront` passed target/security checks and budgets (landing JS 149.01/150.50 kB gzip; CSS 29.63/30.00 kB). `npm run verify:development` passed.

**Unverified and next action:** No production database apply, feature-flag activation, real account/notification, exact-host product header check, real purchase/transfer or physical-device acceptance occurred. MAP-019/020 must first complete the signed guest/account prerequisite chain and guarded provider cutover. MAP-023 needs owner/staff real receiving account checks and a separately verified transfer. MAP-024 must inspect raw and hydrated crawler signals at the exact host. MAP-025 must run customer/staff phone and recovery journeys. Recovery before provider application is a scoped feature-branch revert; after application use the MAP-017/019 controlled backup/change-window procedure and restore the prior Storefront artifact.
