# MAP-017 remaining authorization findings — 9 September 2026

This continuation executes the existing MAP-017 finding-review task. No new
product idea, production mutation, or application change is introduced.

## Evidence identity

- Input: ignored `live-schema-metadata.json`, exported at
  `2026-09-08T15:45:50.45529` (value as supplied by the export).
- SHA-256: `6DF512367AEE17DF737B6C3CC519A6FD9EBF9BE421E95F4DDD7FFBA6BBE89991`.
- `node scripts/schema-truth-audit.mjs --export=live-schema-metadata.json`
  reproduces `NON_CONFORMANT_CRITICAL`: 26 critical findings, zero high.
- Exhaustive coverage reported: 42 public tables, nine public views, 54 public
  functions, 15 schema grants, 96 default-privilege entries.
- This is a repeat audit of the named export, not a fresh provider capture.

## Finding-to-remediation trace

| Findings | Source evidence | Existing remediation and limit |
| --- | --- | --- |
| 2 | Anonymous INSERT and blanket insert policy on `public.error_reports` | `20260826_map017_error_report_boundary.sql` removes both browser INSERT grants and known policies while asserting the staff-read boundary. Separately unapplied. |
| 10 | PUBLIC and anonymous execution on five trigger functions: `reject_event_mutation`, `touch_staff_allocations`, `sync_product_compat_columns`, `sync_product_batch_compat_columns`, `prevent_conversation_event_mutation` | `20260821_function_execute_lockdown.sql` revokes client execution. Catalog ACL findings do not establish that a trigger-returning function is directly callable as an RPC. Actual trigger behavior after revocation still needs composed acceptance. |
| 4 | PUBLIC and anonymous execution on `receive_po(uuid)` and `receive_po_scanned(uuid,jsonb)` | The same function-lockdown migration revokes browser execution and preserves service-role execution. Canonical supplier receipt remains MAP-023 scope. |
| 4 | Anonymous execution on `validate_coupon`, `submit_order_request`, `submit_order_request_v2`, `submit_pasabuy_request` | `20260812_guest_submission_cutover.sql` revokes legacy access and grants the signed guest functions. Follow the Guest Commerce BFF runbook's coordinated cutover; a standalone revoke is not complete commerce acceptance. |
| 6 | `supabase_admin` defaults for anon/authenticated TABLE, SEQUENCE and FUNCTION objects | MAP-017 records Management API `42501` during the earlier rollback-only attempt. Requires a supported provider-authorized path; this run did not repeat it or bypass ownership. |

The 26 rows are finding counts, not 26 independent exploitable functions.

## Newly confirmed applicability dependency

The existing function-lockdown migration also unconditionally revokes privileges
on `sync_product_publication_status()` and `touch_product_intake_session()`.
Neither signature occurs in this export. Both are created by
`20260811_product_intake_and_sku_gate.sql`. Therefore the whole lockdown file must
not be treated as an independently applicable next production payload. It needs
the prerequisite migration or a separately reviewed exact-current-schema slice,
with behavioral trigger and receiving authorization evidence. This is a
source/export dependency finding; no failing production DDL was attempted.

## Fresh local database verification

`node scripts/rehearse-map017-portable.mjs` initially failed at PostgreSQL startup
inside the sandbox. The approved elevated rerun exited 0:

- vulnerable baseline, preflight, rollback restoration and apply;
- 12 executable authorization groups;
- payload-bound apply receipt, verification and idempotent replay;
- error-report boundary applied twice; 100 INSERT attempts per browser role
  (`anon` and `authenticated`) denied, staff read preserved, customer read denied;
- encrypted backup, exact 14-row legacy archive equality and isolated restore.

The runner uses named disposable localhost databases at port 55432 and stops the
server it starts. These results cover phase one and the error-report correction;
they do not establish function-lockdown/guest-cutover composition or provider
default repair. No application build was needed for this documentation change.

Recovery: revert only this continuation's documentation hunks. Production is
unchanged. Remaining actions and sequencing live in MAP-017, not this evidence
record.

## Existing-function correction prepared and verified

`supabase/migrations/20260909_map017_existing_function_lockdown.sql` is a
transactional, seven-signature privilege correction. SHA-256:
`556331F3D65077A99DB98231319341E4415E00FA1AEB0A06F9C48FD2FDADCC68`.
It leaves the broader August lockdown intact for its own dependency sequence.
It neither replaces function definitions nor activates a supplier receipt flow.

The existing portable runner now invokes
`scripts/rehearse-map017-function-lockdown.mjs`. That module extracts the actual
repository definitions from their original supply-chain, launch stabilization,
inbox and custody migrations. The SQL fixture supplies tables, not replacement
function bodies. The two absent intake functions remain absent in the fixture.
It runs entirely in a rolled-back transaction inside the existing disposable
local database. No new provider connection or production apply tool was added.

Fresh evidence:

- `node scripts/rehearse-map017-portable.mjs --baseline-function-lockdown`
  failed as intended: `MAP017_FUNCTION_BROWSER_EXECUTE_REMAINS: anon
  public.reject_event_mutation()`.
- `node scripts/rehearse-map017-portable.mjs` exits 0. It checks PUBLIC and both
  browser roles on all seven signatures, four real browser receiving denials,
  and service-role entry to the actual already-received-order rejection in both
  receiving functions. All five real trigger bodies execute through table DML
  after direct execution is revoked: event immutability, conversation
  immutability, staff timestamp and product/batch compatibility synchronization.
- The correction executes twice without changing function definitions. A
  missing receiving signature aborts rather than silently skipping it. Separate
  connections compare fingerprints of all public function definitions/ACLs
  before and after both failure and successful rollback; they match exactly.
- The same run also passes the original 12 authorization groups, error-report
  denial, phase-one replay, backup, legacy archive equality and restore.
- `npx.cmd playwright test --config=playwright.api.config.js
  tests/map017-authorization.spec.js tests/map017-error-report-boundary.spec.js
  tests/schema-truth-tool.spec.js tests/security-surface-inventory.spec.js
  --workers=1`: **41 passed**. Existing environment emitted only the Node
  NO_COLOR/FORCE_COLOR warning. `git diff --check` passes.

Limits: the trigger fixture uses the named original repository definitions;
it does not prove which later definitions are installed by the provider.
Service-role entry is permission evidence, not successful receiving or inventory
reconciliation. No grants on guest RPCs or provider-owned defaults change.
The new rehearsal is included in the existing portable command used by CI;
no remote CI run or production change is claimed here.

Recovery: before application, remove only the new migration, rehearsal module,
two SQL fixture files and its portable-runner import/call if abandoning this
slice. Preserve prior user edits. After any future provider application, use
reviewed roll-forward recovery; do not restore broad browser execution as a
default rollback. The owning MAP item retains the exact next activation work.

## Fresh provider preflight and combined activation contract

The approved read-only export returned HTTP 201 on 9 September. Export timestamp
`2026-09-09T02:31:15.674184`; SHA-256
`C2ABC48FE738D558EFBDDFDA31DE22FFCB1130D45EC539D6CF033C441EB6F2DA`.
The exhaustive audit still reports the same 26 critical findings. The prior
export identity above remains historical evidence, not the current file hash.

`supabase/map017_followup_verification.sql` was first exercised by the local
rehearsal and then sent to the fixed K2 project through Management API with
`read_only: true`. HTTP 201 returned these exact Boolean results:

| Check | Live result before follow-up |
| --- | --- |
| Seven signatures present / expected return types / postgres ownership | true / true / true |
| Error-report RLS / staff SELECT policy and privilege | true / true |
| Public stock-view SELECT / service-role receiving EXECUTE | true / true |
| PUBLIC / browser function execution removed | false / false |
| Browser error-report INSERT grant / policy removed | false / false |

A current `rg` search for `receive_po`, `receive_po_scanned` and `error_reports`
across `src`, `server`, `prepared-api` and `api` found only the error-report type
declaration. This rules out literal current application callers; it does not
prove absence of external scripts or direct provider clients.

`scripts/map017-followup-contract.mjs` builds the two corrections into one
transaction with five-second lock and thirty-second statement deadlines, an
advisory lock, exact phase-one receipt prerequisite, applicability checks,
all eleven postflight checks and an actual anonymous stock-view SELECT before
commit. The ledger identity is distinct from phase one:

- Project: `pixplcjqivlfflickobf`
- Version: `20260909023000`
- Name: `map017_existing_functions_and_error_reports`
- Artifact SHA-256:
  `7BA3F473C3313890F57899A657CD1234FEDAFEEB43DB7465819919EF1FB71E05`
- Receipt idempotency key: `sha256:` followed by that hash.

The hash binds the transaction builder source and both SQL migrations plus the
verification query. An artifact edit changes the receipt identity. The builder
has no network calls and does not execute SQL. Its independent verification SQL
requires the exact ledger receipt as well as all postflight booleans.

Fresh local verification: the existing portable command passes the combined
transaction twice with exactly one receipt, rejects a conflicting receipt, and
rolls the fixture back. The initial large SQL invocation hit Windows
`ENAMETOOLONG`; `runPsql` now accepts optional standard input and the larger
rehearsal passes SQL through `-f -`. Existing callers remain unchanged. The full
portable authorization/backup/restore run and the same 41 focused contracts pass
after this correction; `git diff --check` passes.

This prepares the removal of 16 of the current finding rows: 14 function-access
rows and two error-report rows. Ten remain assigned to guest cutover and provider
defaults. This predicted count is not post-apply evidence. No provider write ran.

Application/recovery requirements: obtain authorization for this distinct hash,
check the named provider and existing recovery evidence, execute the generated
transaction once, and read the exact receipt/postflight from a separate request.
On response loss read first; never infer failure or submit a new payload. On
missing/conflicting receipt or failed postflight stop mutations and investigate.
Transaction errors roll back; post-commit recovery is reviewed roll-forward,
not regranting broad browser access. Refresh schema metadata, exhaustive audit,
anonymous catalog behavior and required actual-role evidence after application.
