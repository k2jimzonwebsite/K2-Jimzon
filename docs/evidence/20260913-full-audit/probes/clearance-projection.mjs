import fs from 'node:fs';import assert from 'node:assert/strict';import vm from 'node:vm';
const source=fs.readFileSync('src/views/admin/BatchExpiryManagerModal.jsx','utf8');const start=source.indexOf('const saveClearance = async () => {');const end=source.indexOf('\n  const saveReconciliation',start);let batches=[{id:'lot-1',qty:5,reserved_quantity:1}];const provider={batchId:'lot-1',sku:'FIXTURE',approved:true,inventoryStatus:'available',quantityAvailable:4,sellableQuantity:4};
const context={clearance:{id:'lot-1',approved:true},clearanceReason:'Reviewed actual batch',clearanceKeys:{current:new Map()},crypto:{randomUUID:()=> 'fixture-key'},secure:true,setSaving(){},setError(){},setClearance(){},setClearanceReason(){},setBatches:fn=>batches=fn(batches),setLotClearanceBff:async()=>({ok:true,result:provider})};vm.createContext(context);await vm.runInContext(source.slice(start,end)+'\nsaveClearance()',context);console.log(JSON.stringify({providerReceipt:provider,displayProjection:batches[0]}));assert.equal(batches[0].clearance_approved_at,provider.clearance_approved_at,'UI fabricates approval time instead of using the receipt');

