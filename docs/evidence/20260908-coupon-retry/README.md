# Coupon retry continuation — 8 September 2026

Owner request: continue the interrupted coupon work shown in the screenshot.
Authority: IDEA-20260908-01, MAP-028 I-002. This is evidence, not another backlog.

## Result and scope

Resumed the existing CouponManager, shared retained-command hook, Admin actor
key and payment fixture changes. Four response-loss browser cases reproduced
failed return focus; rejection and actor-change tests already passed. Opening
disabled the trigger before AdminDialog's effect captured it (decision autofocus
could also replace it). Capture the trigger in CouponManager before opening;
pass an optional returnFocusRef to AdminDialog, retaining its existing fallback.

Expanded coupon coverage to nondefault discount/spend/limit/end-date/hunt/clue
values, frozen fields during uncertainty, keyboard retry focus and reduced-motion
short-viewport cancellation. Existing checks cover exact request/key equality,
stable default start time, duplicate submit, pending Escape, correction after
409, and actor-change disposal. No new package or provider command was added.

The recovery specs now run under their dedicated configuration from npm test
and are excluded from the base runner. The dedicated config rejects test.only
in CI. Three routing contracts failed first and pass after the correction.

## Fresh local verification

- Browser: `npx playwright test --config=node_modules/.cache/coupon-verification/playwright.config.mjs`
  — 20/20 pass (7 coupon and 13 existing payment/fulfillment/supplier cases),
  2.4 minutes. The ignored temporary config imports playwright.payment.config.js
  and overrides only port/base URL 5195 to 5295, explicit repo test/server paths,
  and its output directory. Port 5195 was occupied. Normal reproduction with
  that port free: `npm run test:payment-ui`.
- Contracts: `npx playwright test --config=playwright.api.config.js tests/payment-recovery.spec.js tests/admin-command-retry.spec.js tests/admin-bff-contract.spec.js tests/admin-dialog-contract.spec.js tests/product-intake-contract.spec.js tests/release-ci-contract.spec.js`
  — 94/94 pass, 3.7 seconds.
- `npm run build:admin` — pass, including prebuild checks, 40-module artifact
  boundary, 187.21/300 kB minified Admin application budget and 75-file emitted
  secret scan. Initial browser run needed installed-browser filesystem access;
  the sandboxed launch failures were environment failures, not product evidence.
- Phone/landscape PNGs alongside this file were generated by the browser tests.
  Creation phone, archive uncertainty, creation and decision landscape images
  were visually inspected: established dark dialog, readable labelled fields,
  focused recovery/cancel controls, scrollable form, no horizontal page overflow.
  The fixture omits the authenticated Admin shell; its background is not a
  production appearance claim. No new animation was introduced.

## Limits, recovery and next action

Fabricated register and intercepted HTTP responses test the actual client UI
and request serialization, not a real committed coupon, server receipt, audit
event, checkout redemption or deployed staff authorization. The actor switch is
a fixture remount; the production Admin actor/role key was source-reviewed.
The complete aggregate npm test/remote CI and physical-device acceptance were
not run. No database migration, production flag, provider state or deployment
was changed.

Rollback uses `docs/design-checkpoints/20260908-coupon-retry/README.md` and its
pre-edit CouponManager copy. Reverse only the coupon dialog/helper classification,
actor key, fixture/tests, optional AdminDialog returnFocusRef and recovery test
routing changes if withdrawing this slice. Preserve earlier payment, packing,
supplier and unrelated dirty-tree work; never reset the whole working tree.

I-002 remains active. Exact next local action: Customers.jsx wholesale review
retained command identity and canonical updatedAt handling, then media/intake
lifetime adoption. Target-host signed receipt/reconciliation, actual legacy
coupon reconciliation and full-workspace navigation acceptance remain there.
