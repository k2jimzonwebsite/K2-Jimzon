# Provider readiness browser check, 24 September 2026

## Request and scope

The owner asked for a browser check of the signed-in K2 account, Hostinger DNS, Vercel, Supabase and GitHub, and for safe remaining configuration work. This is a read-only provider inspection. No DNS, database, Auth, Vercel, GitHub, payment or deployment setting was changed. These provider pages are evidence of the state they displayed during the check, not proof of a completed customer purchase or an indexed search result.

## Observed provider state

| Provider | Browser observation | Readiness conclusion |
| --- | --- | --- |
| Hostinger | Account menu showed `k2jimzonwebsite@gmail.com`. `k2jimzon.com` was Active, expiring 27 August 2027. Nameservers were `cosmos.dns-parking.com` and `nova.dns-parking.com`. DNS had `www` CNAME `f683b7ff3d09cb06.vercel-dns-017.com`, `admin` CNAME `be6a2ad6b5b189c6.vercel-dns-017.com`, and apex A records `216.198.79.1` and `64.29.17.1`, all TTL 300. | Web records match the recorded Vercel configuration. The visible zone had no MX or TXT records. Do not invent mail or webmaster-verification records; obtain exact provider values first. |
| Vercel | K2 Jimzon team showed separate Ready production projects `k2-jimzon` and `k2-jimzon-admin`, both linked to `k2jimzonwebsite/K2-Jimzon`. The Storefront domains page showed `www.k2jimzon.com` Valid Configuration, apex Valid Configuration with 308 redirect to `www`, and its project Vercel domain Valid Configuration. The Admin domains page showed `admin.k2jimzon.com` and its project Vercel domain Valid Configuration. Both latest production views displayed source commit `2d6f7cfd9a380bce5871064de7044c28eefbe346`, a documentation-only descendant of the released code commit. | DNS and project domain assignment are ready. This does not activate the locally prepared account/notification work or prove every exact-host route. |
| Supabase | Profile menu showed `k2jimzonwebsite@gmail.com`. K2 production project `pixplcjqivlfflickobf` showed Healthy. The migrations page listed latest `20260921033348 admin_globe_direct_rpc`, plus applied `20260909023000 map017_existing_functions_and_error_reports` and `20260824143000 map017_public_write_boundary_hardening`. It did not list the prepared MAP-019/020 guest/account chain or the 24 September account settings and notifications migration. The overview said `GitHub: No repository connected` and `Last backup: No backups`. Auth Site URL is `https://www.k2jimzon.com`, with `/**` redirects for `www`, apex and Admin. Email and Google providers are enabled, but `Allow new users to sign up` is off. The backup page says Free Plan does not include project backups; point-in-time recovery requires a paid Pro add-on. | Production is running, but guest/account cutover, new customer sign-up and provider backup/restore proof remain open. A missing Supabase GitHub integration is informational because the project uses a controlled migration/runbook path; do not connect it casually. Keep sign-up off until the reviewed customer flow and authorization boundary are ready. |
| GitHub | Signed-in account menu showed `k2jimzonwebsite`. Repository `main` was at `2d6f7cfd9a380bce5871064de7044c28eefbe346`; the last code CI run, `35955278280` for `5494b2bf71f7ecf30422e2b8c5bfc246b895bc9f`, showed completed successfully. Repository page displayed `Your main branch isn't protected`. Settings showed no classic branch protection or rulesets. A proposed active ruleset named `Protect main history` targeted `Include: Default`, enabled only `Restrict deletions` and `Block force pushes`, and had an empty bypass list. Clicking Create opened GitHub's `Confirm access` dialog asking to verify identity via email, so no rule was created. | Released code has passing remote CI and both Vercel projects show Ready. GitHub branch protection remains absent until account email verification and a successful save. CI requirements remain a separate design and safe verification step. |

## Limits and next actions

1. MAP-024: Supabase Auth Site URL and three redirect patterns are configured, but exact-host callback tests remain. The public sitemap returned HTTP 200 XML and listed Home, Catalog, Pasabuy and Trade only. `robots.txt` returned HTTP 200 and pointed to that sitemap. All four listed routes returned HTTP 200; Admin, account and a sample product returned `X-Robots-Tag: noindex, nofollow`; an unknown route returned 404. Search Console property ownership, submission and indexing remain unverified. Browser auto-review denied opening signed-in Search Console pending explicit Google-service authorization. The Chrome extension blocked the public sitemap view, so these exact-host checks used read-only public HTTPS requests.
2. MAP-017/019/020: reconcile the pending migration chain with the current live schema, prepare backup/rollback and matching BFF secrets, then apply and activate in dependency order. Leave the existing direct chat path in place until its replacement is verified.
3. MAP-022: the signed-in Supabase backup page explicitly says the Free plan has no scheduled project backups, while point-in-time recovery is a paid Pro add-on. Select and verify an acceptable owner-controlled backup and restore path before the database cutover; include Storage. Do not treat a local export or an untested upgrade as restore evidence.
4. MAP-028: finish GitHub's account email verification through the signed-in owner's normal flow, then create and verify the narrow default-branch deletion/force-push rule. The prepared form may be lost if the session expires. Do not treat the proposed form as an applied setting. Separately identify the applicable CI checks and verify a workable release path before requiring them.
5. MAP-023/025: verify the real QR recipients, stock, transfer, independent payment review and staff/customer journeys. The provider dashboard checks do not close those items.

Opening Gmail was rejected by automatic browser review because its private mail content was outside the requested provider checks. Hostinger and Supabase exposed the exact K2 email in their account menus, while GitHub exposed the matching username. No mailbox content was inspected.

Vercel showed both production projects Ready at source commit `2d6f7cf`. The Storefront project's environment list contained its Supabase URL/publishable key and BFF variable names, but no Google Analytics measurement variable. Values were not copied or recorded. Its overview offered to enable Vercel Analytics, indicating that product is not yet enabled. The repository search found no GA4 tag or measurement ID; this does not prove the Google Analytics account has no property. Do not insert an unapproved tracking tag or transmit customer details to analytics.

No application test suite or build was run for this read-only provider and documentation pass. Recovery for this pass is to revert only these documentation edits; there is no provider rollback.
