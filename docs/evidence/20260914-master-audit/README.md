# Master audit evidence — 14 September 2026

Decision: IDEA-20260914-01. Remaining work: root MASTER_ACTION_PLAN.md, MAP-028 J and its existing linked owners. [Audit report](../../audits/MASTER_PROJECT_AUDIT.md).

Baseline is the pre-existing dirty working tree over commit `41d96df012997cc98751ca405ab632f95ae8806f`. The source manifest hashes 762 source/test/SQL/script files (113,648 lines); this is inventory, not exhaustive manual line coverage. No application source, migrations, dependencies, credentials or production state were changed by this audit.

## Fresh execution

All commands ran from the repository root. Logs are local receipts, not deployed-host acceptance.

| Command | Receipt | Result |
| --- | --- | --- |
| `npm run test:base` | [test-base.log](test-base.log) | Exit 0; 771 passed, 9.2 minutes |
| `npm run test:admin-ui` | [admin-ui.log](admin-ui.log) | Exit 0; 32 passed, 2.8 minutes |
| `npm run build:storefront` | [build-storefront.log](build-storefront.log) | Exit 0; target boundaries, secrets and budgets passed |
| `npm run build:admin` | [build-admin.log](build-admin.log) | Exit 0; target boundaries, secrets and budgets passed |
| `npm run security:gate` | [security-gate.log](security-gate.log) | Exit 0; scanner regressions, environment/file policy, tree and history |
| `npm run security:surfaces` | [security-surfaces.log](security-surfaces.log) | Exit 0; 92 Admin / 15 Storefront / 2 Edge routes; no source classification gaps |
| `npm audit --json` | [dependencies.json](dependencies.json) | Exit 0; zero reported vulnerabilities, 278 dependencies |
| `node docs/evidence/20260914-master-audit/probes.mjs` | [probe-results.json](probe-results.json), [probes.log](probes.log) | Exit 0; targeted synthetic behavioral observations |
| `node docs/evidence/20260914-master-audit/routes.mjs` | [route-matrix.json](route-matrix.json), [routes.log](routes.log) | Exit 0; 110 initial/recovery route/viewport measurements |
| `node docs/evidence/20260914-master-audit/inventory.mjs` | [inventory.json](inventory.json), [source-manifest.json](source-manifest.json) | Exit 0; source and asset inventory |

The base suite completed its assertions but its Vite web-server teardown stalled. Only the two Vite processes identified from this audit's launch logs were stopped; the test runner then exited 0. This intervention is not hidden in the passing result. The full aggregate `npm test` command was not run; dedicated suites beyond those listed are not claimed fresh.

The separate builds ran sequentially into the shared local `dist` directory; its final contents are the Admin build. Both remain independent production artifacts. npm audit initially lacked network access and passed after an approved retry; no install or upgrade occurred.

## Synthetic browser scope and reproduction

The browser probes require a local Vite development server at `http://127.0.0.1:5199`, configured with synthetic development Supabase settings. Inspect the scripts before replaying. They abort non-loopback traffic and intercept local REST/API calls; use only a disposable local development session. The scripts import actual cookie/signing and customer aggregation helpers, but provide synthetic client responses. Browser launching required an approved sandbox escalation.

The product fixture, contact details and bot token are synthetic. Checkout's lost response and subsequent 409 are simulated to observe retained key/token and changed request payload; the SQL fingerprint behavior is separately established from source. The 2,000-order fixture demonstrates capped metrics being labeled available, not an actual production customer with that history. Route measurements cover rendered initial/recovery states, not every interaction or authenticated operational journey.

Screenshots: [product phone](product-390.png), [product desktop](product-1440.png), [Admin desktop](desktop.png), [Admin mobile metrics](mobile-metrics.png), [Admin mobile sales](mobile-sales.png). Admin screenshots generated by the existing suite were copied here, and its three older screenshot files were restored from pre-run backups. No existing evidence was intentionally replaced.

## Dated and unavailable evidence

- Production permission evidence is from [13 September remediation](../20260913-audit-remediation/README.md), not a fresh live query. It records an applied migration and remaining grants/default privileges. Source classification cannot prove current production ACLs.
- Automatic approval review rejected the fresh `export-live-schema-metadata.mjs` invocation before execution, citing insufficient authorization for sensitive schema metadata involving the Supabase destination. No live query ran. Next: obtain explicit authorization for the read-only metadata query on K2 project `pixplcjqivlfflickobf`, then compare its receipt under MAP-017 / MAP-028 J. Do not bypass the rejection or repeat migrations.
- No fresh SQL reset/rehearsal, restore, real payment, inventory mutation, provider activation, deployment or credential rotation was performed. Historical SQL receipts remain dated evidence only.
- Physical devices, screen readers, comprehensive contrast/zoom/keyboard-overlay testing, field vitals, query plans, load tests, remote CI and exact-host end-to-end acceptance remain unverified.
- The installed audit-context-building skill's referenced `OUTPUT_REQUIREMENTS.md` and `COMPLETENESS_CHECKLIST.md` were absent. Its main checklist, owner audit requirements and repository evidence rules were used; no claim of following those unavailable supplemental files is made.

## Handoff and recovery

Final integrity checks: all 762 source-manifest hashes still match; all three
restored historical Admin screenshot hashes match their pre-run backups;
`git diff --check` passed (only a Git line-ending normalization warning);
`node scripts/scan-secrets.mjs docs/evidence/20260914-master-audit` passed.

Fourteen findings (six P1, eight P2) are documented with confidence and existing owners in the report. Remaining actions live only in root MAP-028 J and the linked MAP items. Audit changes comprise the report, these evidence/probe files, FUTURE_IDEAS decision, System Brain current-state note and MAP reconciliation. Revert only this audit's additions if needed, preserving unrelated dirty work. No database rollback is required.
