# Account and permission migration rehearsal, 24 September 2026

## Request and boundary

The owner asked to proceed with the practice run before customer registration is activated. This pass used an isolated loopback PostgreSQL 17.11 database and read-only production metadata queries for project `pixplcjqivlfflickobf`. It did not apply production SQL, change Auth sign-up, change Vercel flags, create a customer, or deploy code.

## Current production starting point

Supabase's migration ledger still ends at `20260921033348 admin_globe_direct_rpc`. Read-only SQL found no `public.customers`, guest grant/scopes, customer account, settings, or notification tables. Required existing order, Pasabuy, conversation, message, user-profile tables and `public.is_staff()` are present; zero conversations have an existing customer ID. `submit_order_request_v2` has the expected eleven-argument signature with three trailing defaults, the Pasabuy command exists, and the legacy direct chat command still has anonymous execute permission. The order request fingerprint and Pasabuy idempotency columns are absent, as expected before the MAP-019 identity migration. These checks compare named prerequisites only; they are not a complete live schema export or an apply/rollback trial against production data.

The repository's read-only metadata exporter then fetched the complete current catalog inventory through the Supabase management API, reporting 99 tables, 12 views, 162 functions, 132 object grants, 15 schema grants, 96 default-privilege entries and 8 migration entries. It selected no customer rows and wrote the raw export to ignored `.tools/account-rehearsal-live-schema-20260924.json`. `schema-truth-audit.mjs` compared that export with the repository contract. Its [saved report](schema-audit.md) records 18 findings: 13 critical and 5 high. This supersedes the older ten-critical count. The report flags anonymous direct functions, one PostgreSQL PUBLIC function grant, six provider-owned default-privilege groups and five authenticated functions without explicit reviewed contracts. The audit's severity is a triage signal; each function needs classification before changing grants because some are still used by the current storefront or Admin.

## Follow-up classification and local correction

The five high findings were a missing audit inventory for live authenticated functions. Source and read-only production metadata agree that `read_admin_globe_cms_v1` and both Globe review commands enforce Admin plus AAL2, while `website_reply_capability_v1` and `append_website_customer_reply_v1` enforce Staff. Their exact grants are authenticated only, with no anonymous execute. The direct Globe and website-reply functions are transitional while the Admin BFF remains off. `scripts/schema-truth-core.mjs` now records those five contracts and their cutover disposition. The [after-review report](schema-audit-after-review.md) still flags 13 critical findings and zero high findings against the same captured export. This is contract classification, not a production permission change.

The remaining seven function findings have different dependencies:

| Function group | Current use and correction boundary |
| --- | --- |
| `get_public_product_stock()` | Public stock projection must remain callable by `anon` and `authenticated`, but its extra PostgreSQL `PUBLIC` execute grant should be revoked in a scoped, backed-up change. The live ACL confirms explicit grants to the required roles already exist. |
| `get_storefront_chat_v1(uuid)` | Live chat resumes by conversation UUID through this anonymous RPC. Its direct read is not a verified ownership boundary. Replace it with the signed guest conversation path and prove continuity before revoking anonymous execute. |
| `validate_coupon`, `submit_storefront_chat_v1`, `submit_order_request_v2`, `submit_pasabuy_request` | The current Storefront calls these direct RPCs while the guest BFF flag is off. Keep their grants until signed routes, preview denials and a coordinated customer release pass. |
| `submit_order_request` | No current Storefront source call was found, but an older client can still call this anonymous legacy function. Classify compatibility and revoke at the controlled guest cutover. |

The other six critical findings are `supabase_admin` default privileges for future public functions, tables and sequences. The read-only connector runs as `supabase_read_only_user`, is not a member of `supabase_admin`, and cannot correct that role's defaults. [Supabase's current API security guide](https://supabase.com/docs/guides/api/securing-your-api) explains explicit grants and a `postgres` default-privilege opt-in; it does not establish that K2's `supabase_admin` groups can be changed by this connector. Use a provider-supported owner path, then re-export and audit. Do not attempt to impersonate a provider role.

A separate read-only MAP-017 stock ACL verification now detects an explicit or default PostgreSQL `PUBLIC` execute grant while requiring the intended `anon` and `authenticated` access. An initial edit to the historical phase-one postflight changed its receipt hash and caused the isolated follow-up gate to refuse it. That edit was reverted; the new check is outside the hash-bound migration artifacts. The captured live ACL still has the grant and no production revoke was run. The current iteration count in the active MAP was corrected from the stale ten to thirteen.

## Executed local evidence

| Command | Result |
| --- | --- |
| `npm run rehearse:map019-account-claim` | Passed in isolated PostgreSQL after the restricted sandbox could not start PostgreSQL. The permitted loopback rerun proved rollback restored baseline, then migration apply, postflights, account claim and settings/notification assertions, product behavior, and migration replay. |
| `node scripts/rehearse-anonymous-chat-moderation.mjs` | Passed the current eleven-argument order preflight fixture, isolated moderation apply and behavior, and rollback restoring the old direct-chat grant. This does not prove the entire guest migration chain on production. |
| `npm run verify:map020-storefront-auth-rate-portable` | Passed private rate-budget, denial persistence, replay, privilege, postflight and migration replay checks. |
| `npm run verify:map017-portable` | Passed artifact checks and isolated permission, rollback, replay, backup and restore rehearsal. Its fabricated clean schema fixture is parser evidence, not a live clean audit. |
| `npm run verify:map019-identity` | Passed the static hybrid identity contract. |
| `npm run verify:guest-bff` | Passed after its stale fixed count of 15 routes was replaced with a uniqueness check against the actual 17-route inventory; the two new account routes were added to its sensitive-source scan. The original failure was `17 !== 15`. |
| `npm run verify:development` | Passed after the final code edit, including source-surface inventory and secret/import checks. The final run reported 17 prepared Storefront routes and no route-control classification gaps. |
| `node scripts/map017-evidence/export-live-schema-metadata.mjs .tools/account-rehearsal-live-schema-20260924.json` | Read-only production metadata export returned HTTP 201. No customer rows or credentials were selected. |
| `node scripts/schema-truth-audit.mjs --export=.tools/account-rehearsal-live-schema-20260924.json --allow-findings --output=.tools/account-rehearsal-schema-audit-20260924.md` | Completed and reported 18 findings. The report was copied to `schema-audit.md`; `--allow-findings` allows triage output and does not mean conformance. |
| Re-audit of the same export after the five reviewed contracts were recorded | Completed with 13 critical, 0 high. The report was copied to `schema-audit-after-review.md`; no live grant was changed. |
| `npx playwright test --config=playwright.api.config.js tests/schema-truth-tool.spec.js` | The first run found one stale test that expected the phase-one payload hash in the slim active MAP. The test now checks the active backup runbook, where the exact hash belongs. Rerun passed 24/24. |
| `npm run verify:map017-portable` after adding the independent stock ACL check | First isolated run refused the follow-up because an interim historical postflight edit changed the phase-one hash. The edit was reverted and the new check moved to `supabase/map017_stock_public_grant_verification.sql`. Rerun passed all 12 authorization groups, rollback, replay, encrypted backup and isolated restore; the phase-one hash again matched `D1E1EAA0696F12BF467584016A5013B655BB074D44D2A52AFF3951B335EBDB62`. |
| `npm run verify:development` after the final code edit; `git diff --check` | Passed. The historical phase-one postflight has no working-tree diff. |

## Remaining gate and recovery

The isolated account fixture is a selected schema, not a restored copy of the current production database. Before a live migration, close or explicitly accept the 13 still-critical findings, reconcile the full MAP-017/019/020 dependency chain with the captured catalog, perform the runbook's rollback-only current-schema window, obtain a verified database and Storage recovery path, and prepare matching private BFF secrets, Turnstile and preview denials. Keep direct chat and both customer/guest BFF switches off until the coordinated cutover is proven. Supabase new-user sign-up is still off, so no live registration claim is made.

The local rehearsal servers shut down after each runner, confirmed by `pg_ctl status` for both isolated clusters. The code changes in `scripts/verify-guest-bff.mjs`, `scripts/schema-truth-core.mjs` and `tests/schema-truth-tool.spec.js` are locally reversible; no provider rollback is needed for this pass. The exact next action belongs to MAP-017/019/020: prepare the scoped stock-function grant correction and the provider-supported default-privilege path, then run the ordered guest/account chain in a rollback-only current-schema window with backup and no customer-facing flag change.

## Continued cutover repair

The owner asked to keep fixing the open permission work. A review of `20260812_guest_submission_cutover.sql` found that its direct order revoke named only the old nine-argument function, while production has an eleven-argument function with defaults. The unapplied cutover now checks for unexpected overloads and revokes both known signatures when present, inside its transaction. The isolated PostgreSQL fixture exercised both overloads, confirmed anonymous and authenticated denial after cutover, and confirmed the signed guest order and conversation entry points stayed callable. `node scripts/rehearse-anonymous-chat-moderation.mjs` and `npm run verify:guest-bff` passed. The first `npm run verify:development` exposed a source-audit gap with a dynamic revoke; the cutover was changed to exact guarded revokes, then the focused checks and final development gate passed. No production SQL or flags changed.

Read-only production role inspection showed `postgres` is not a member of `supabase_admin`, while `supabase_admin` is a member of `postgres`. K2's current SQL connector is also read-only. The six provider-owned default-privilege findings therefore need a provider-supported owner path; running `ALTER DEFAULT PRIVILEGES FOR ROLE supabase_admin` as K2's current role is not a supported correction. A local Supabase CLI was not installed, and an attempted `npx supabase --version` did not resolve in the restricted environment. No new migration file or live grant change was made from that attempt.

The live stock `PUBLIC` execute grant and legacy direct chat grants remain. The stock correction is independently scoped; the fresh backup and local restore below are now available, while the current-schema permission rehearsal and controlled provider write remain open. Direct chat cannot be revoked until the signed guest path preserves the existing buyer conversation or a reviewed recovery path is chosen; a stored UUID alone does not prove account ownership. The prepared order cutover repair prevents an eleven-argument bypass once the coordinated guest switch is ready. Recovery for this local repair is to revert only the cutover and fixture changes; no provider rollback is needed.

## Current backup and provider support receipt

The fresh application database backup `current-pixplcjqivlfflickobf-2026-09-24T094715954Z-883ee90e72a2` was created with `npm run backup:current-production` and restored into the dedicated loopback database `k2_current_restore_20260924` with `npm run verify:current-production-restore`. The restore receipt records 51 public relations, latest migration `20260921033348`, the matching 14-row legacy fingerprint, and ten excluded managed Vault entries. The encrypted envelope is 824,951 bytes with SHA-256 `883ee90e72a2e84215ca455d30bc6d59591a6208566620cc480b4183056c961d`. The adjacent manifest retains `restoreVerification: Pending`; the separate restore receipt is the verification evidence. This is an isolated application-database restore, not a production rollback or managed Vault restore.

The fresh Storage backup `map017-storage-pixplcjqivlfflickobf-2026-09-24T094916953Z-d325b1360c7f` captured all 36 public `product-images` objects, 115,573,916 source bytes. `verify:map017-storage-restore` restored and re-read every object in a dedicated local directory; the count, bytes and aggregate fingerprint matched. Its encrypted archive is 115,580,694 bytes with SHA-256 `d325b1360c7f47aa34ce4ede8a99448e6092dcb43c543da2143a21a936107e06`. It was split into 67,108,864-byte and 48,471,830-byte transport parts, and local reassembly matched the original encrypted digest. Bucket policies and provider settings were not included.

The two encrypted backups, redacted manifests, restore receipts and Storage parts manifest were uploaded as eight files to the existing owner-only Google Drive folder `K2 Production Backups` (`1mQuU8Jj6eWhDr-lpZV3YJDtaEwfAh8yo`) under `k2jimzonwebsite@gmail.com`. A post-upload metadata read for every file confirmed the folder parent, exact local byte length, `shared: false`, and only the K2 account as owner. The database envelope was subsequently fetched and hashed as recorded below; independent retrieval of the other new files remains open. Local ignored paths are `.tools/current-production-backups/pre-account-cutover-20260924.k2backup` and the matching `.k2storage` files. The database and Storage restore receipts sit beside them.

With the owner's authorization, a Supabase support request was submitted from the signed-in K2 organization for the six `supabase_admin` default-privilege groups. The dashboard displayed **Support request sent** and said the ticket was logged for K2jimzon, with replies to `k2jimzonwebsite@gmail.com`; it supplied no ticket number in the visible confirmation. The form's optional project support-access switch was off. The request asks for a supported correction path that preserves existing explicit grants and platform behavior; it included no credentials or customer rows. Await the provider response before changing those defaults.

After the backup command and cutover code edits, the single final `npm run verify:development` run passed, including the source boundary and secret scans; `git diff --check` returned no whitespace errors. These checks do not establish a successful deployed cutover.

## Current-schema rollback-only follow-up

The newly uploaded database envelope was independently fetched from Google Drive as raw bytes. The returned 824,951 bytes hashed to SHA-256 `883ee90e72a2e84215ca455d30bc6d59591a6208566620cc480b4183056c961d`, exactly matching the locally authenticated encrypted envelope. This verifies retrieval of the new database copy. The two large Storage transport parts have not yet passed independent Drive download and checksum comparison.

The restored `k2_current_restore_20260924` database passed `supabase/map019_identity_preflight.sql`. An ignored rollback-only SQL assembly then ran, in order, the MAP-019 identity migration and postflight, MAP-020 guest preflight and signed submission migration and postflight, account claim migration and postflight, customer Auth boundary, guest order status, customer settings and notifications, then the repaired direct-RPC cutover and its postflight. Each source migration's outer `begin`/`commit` was removed for this one enclosing transaction. `psql` exited 0; the account-claim postflight printed its pass marker. The rehearsal script SHA-256 was `155FFB0E4F8DAEB9D00FAEF823F8CB3561B80693654B018B9F9B75F0BA7BD1D6`. Its final `ROLLBACK` restored the baseline: `public.customers`, the private settings table and signed guest order function were absent again; the latest migration ledger remained `20260921033348`; public relation count remained 51. The local PostgreSQL server was stopped afterward.

The first restore used `pg_restore --no-privileges`, so a second isolated database was created to exercise the archived ACLs. That first grant-preserving restore stopped on missing Supabase-managed role names. Local non-login placeholders were created for `supabase_auth_admin`, `dashboard_user`, `supabase_storage_admin`, `supabase_realtime_admin`, `authenticator` and `pgbouncer`; no role or permission was changed on Supabase. The separate `k2_current_acl_restore_20260924` database then restored the archived ACLs with only the same ten managed Vault TOC entries excluded. Its baseline had 51 public relations, `PUBLIC` and `anon` execute on `get_public_product_stock()`, and anonymous execute on the eleven-argument direct order command, matching the named live findings.

The ordered rollback-only chain was rerun against that grant-preserving restore. Its postflights denied direct order, Pasabuy and coupon execution after cutover; an added assertion confirmed all six signed guest entry points remained callable by `anon` and the old direct chat read remained available for separate continuity review. `psql` exited 0 and printed `SIGNED_GUEST_GRANTS_AND_LEGACY_CHAT_PRESERVED`. The final SQL assembly SHA-256 was `3ED1853724BCF84C44220D8C64BAAF508D6D1F03CBC46916BEA497F42DFD0C1B`. After rollback, customers and signed guest objects were absent again, anonymous direct order execution and the stock `PUBLIC` grant returned, the ledger still ended at `20260921033348`, and public relations remained 51. The local server was stopped.

This proves SQL compatibility and the named grant transitions on a restored current application database. The placeholder roles do not recreate Supabase's managed role memberships or provider-owned defaults, and Vault/provider configuration remains excluded. It also does not prove signed BFF secrets, Turnstile, preview browser behavior, live chat continuity, Supabase Auth sign-up, or a production apply. Keep those MAP-017/019/020 gates open.
