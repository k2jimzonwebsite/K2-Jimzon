# Human testing release candidate, 25 September 2026

Owner request: finish the locally prepared work that can safely join production-linked `main`, then begin human testing. Owning backlog: MAP-017 through MAP-025 and MAP-028. This record separates source release from provider activation and operational acceptance.

The isolated branch `codex/human-testing-release` starts at GitHub `main` commit `e6fe012`. It carries the prepared customer account, profile and reference-only notification source behind disabled account/guest BFF switches; Storefront receipt, payment copy, allergen and indexing corrections; the current-schema security rehearsal and backup tooling; and the older seven-stage guided manual intake and contextual dashboard Help pilots merged into the current Admin navigation. The account, guest cutover and notification SQL is committed as prepared source only. This release does not apply those migrations, change production flags, verify payment receipt, approve physical stock or publish a product.

Verification on the isolated branch: 24 focused account, copy, indexing and guide contracts passed; customer account UI fixtures passed 4/4; `npm run verify:development` passed after the final test edit. The final `npm run verify:release` exited 0. Its base suite passed 962/962; mobile moderation 4/4; Store orientation 12/12; Storefront UI 31/31; recovery 14/14; Admin UI 41/41; payment UI 36/36; Inbox 32/32; Admin product master 2/2; Owner Count & Close 1/1; customer account 4/4; selling surfaces 13/13; workflow API 4/4; intake AI 23/23. Separate Storefront and Admin builds, boundary checks, budgets and secret scans passed. Admin entry measured 224.71/300.00 kB minified.

Two prior gate attempts were inconclusive, not product failures. The first stopped when Windows returned `ERR_NO_BUFFER_SPACE` opening a local payment fixture; that one test passed in isolation and the final full payment suite passed. The second exposed an incomplete manual-intake test fixture: after a Draft, the form normally reads open consignments for step 6, but the fixture did not answer that request. Adding the expected empty reply made the focused manual path and final 23-test intake suite pass.

Production and human evidence are pending until the exact commit is pushed, both Vercel deployment receipts succeed, and the owner/staff run real device and data journeys. Customer registration, profile and notifications cannot be called live while their database and BFF gates remain off. The guest order request and Admin intake may be rehearsed on the released site, but real stock, payment, dispatch and independent fund verification stay under MAP-018/023/025.

Recovery before a provider change: revert the scoped Git commit and restore the prior successful Storefront/Admin deployments. For later Supabase migrations or flags, follow the ordered backup, apply, deny, postflight and rollback procedure in the database and guest BFF runbooks; this Git receipt is not migration authorization.
